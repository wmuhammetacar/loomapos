# Release Foundations

- Desktop update feeds are versioned by channel.
- Mobile version policy is served separately from store binaries.
- Compatibility matrix defines minimum supported client versions per backend release window.
- Release metadata is immutable after publication and tied to CI build output.
